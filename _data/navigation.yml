# The following is the order of the links in the header of the website.
#
# Changing the order here will adjust the order and you can also add additional
# links. Removing a link prevents it from showing in the header, but does not
# prevent it from being included in the site.
#
# Template-cleanup note: every demo navigation entry that shipped with the
# academicpages template (Publications, Talks, Teaching, Portfolio, Blog
# Posts, CV, Guide) has been removed, because none of those sections are
# supported by the memory-backed content of this site. This repository is a
# single-page template-cleanup report, so the header navigation is
# intentionally left empty.

main: []
